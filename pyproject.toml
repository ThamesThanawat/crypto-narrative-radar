[project]
name = "crypto-narrative-radar"
version = "0.1.0"
description = "Market intelligence project for crypto narrative momentum analysis"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
  "pandas",
  "requests",
  "streamlit",
  "plotly",
  "jinja2",
  "python-dotenv",
]
